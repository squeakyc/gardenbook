<%= form_with(model: @garden, local: true) do |form| %>
  <% if garden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(garden.errors.count, "error") %> prohibited this garden from being saved:</h2>
      <ul>
      <% garden.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <%= link_to "Back", gardens_path, class: "btn btn-info" %>
    <div class="row">

      <div class="col">
      </div>

      <div class="col">


        <div class="form-group" id="new-garden-data-entry">
          <p><strong>Name:</strong></p>
          <%= form.text_field :name, placeholder: "Garden Name", autofocus: true, class: 'form-control' %>
        </div>

        <div class="form-group" id="new-garden-data-entry">
          <p><strong>Description:</strong></p>
          <%= form.text_area :description, placeholder: "Description", class: 'form-control' %>
        </div>

        <div class="form-group" id="new-garden-data-entry">
          <p><strong>Upload an Image:</strong></p>
          <%= form.file_field :image %>
        </div>

        <div class="actions" id="new-garden-data-entry">
          <%= form.submit "Save", class: "btn btn-success btn-block" %>
        </div>
      </div>

      <div class="col">
      </div>

    </div>
  </div>

<% end %>



<br><br>
